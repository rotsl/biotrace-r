% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{biotrace_workflow}
\alias{biotrace_workflow}
\alias{biotrace_workflow_text}
\alias{write_biotrace_workflow}
\alias{use_biotrace}
\title{BioTrace workflow scaffolding}
\usage{
biotrace_workflow_text(
  path = NULL,
  config_path = ".github/biotrace.yml",
  fail_on = "error",
  ai_enabled = "false",
  include_workflow_dispatch = FALSE,
  upload_report = TRUE,
  extra_inputs = list()
)

write_biotrace_workflow(
  path,
  config_path = ".github/biotrace.yml",
  fail_on = "error",
  ai_enabled = "false",
  include_workflow_dispatch = FALSE,
  upload_report = TRUE,
  extra_inputs = list(),
  overwrite = FALSE
)

use_biotrace(
  project = NULL,
  config_path = ".github/biotrace.yml",
  workflow_path = ".github/workflows/biotrace.yml",
  fail_on = "error",
  ai_enabled = "false",
  include_workflow_dispatch = FALSE,
  upload_report = TRUE,
  extra_inputs = list(),
  overwrite = FALSE
)
}
\arguments{
\item{path}{\verb{[character(1)]} For \code{\link[=write_biotrace_workflow]{write_biotrace_workflow()}}, the
path to write. For \code{\link[=biotrace_workflow_text]{biotrace_workflow_text()}}, ignored.}

\item{config_path}{\verb{[character(1)]} Path (relative to \code{project})
of the BioTrace YAML configuration file. Defaults to
\code{.github/biotrace.yml}.}

\item{fail_on}{\verb{[character(1)]} Minimum severity that causes the
Action to fail. One of \code{"info"}, \code{"warning"}, \code{"error"}. Defaults
to \code{"error"}.}

\item{ai_enabled}{\verb{[character(1)]} Whether AI observations are
allowed. One of \code{"auto"}, \code{"true"}, \code{"false"}. Defaults to
\code{"false"} so the workflow is fully deterministic by default.}

\item{include_workflow_dispatch}{\verb{[logical(1)]} Add a
\verb{workflow_dispatch:} trigger so the workflow can be run
on-demand from the Actions tab. Defaults to \code{FALSE}.}

\item{upload_report}{\verb{[logical(1)]} Upload the BioTrace JSON
report as a workflow artifact. Defaults to \code{TRUE}.}

\item{extra_inputs}{\verb{[named list]} Extra \verb{with:} inputs to pass
to \code{rotsl/biotrace@v1}. Each name becomes a YAML key. Use this
to set \code{create-labels}, \code{comment-mode}, \code{report-path},
\code{ai-provider}, etc.}

\item{overwrite}{\verb{[logical(1)]} Overwrite existing files. Defaults
to \code{FALSE}.}

\item{project}{\verb{[character(1)]} Project root. Defaults to the
current working directory, or the nearest directory containing
\code{.git} or \code{DESCRIPTION}.}

\item{workflow_path}{\verb{[character(1)]} Path (relative to \code{project})
of the GitHub workflow YAML file. Defaults to
\code{.github/workflows/biotrace.yml}.}
}
\value{
\itemize{
\item \code{use_biotrace()}: a named list of absolute paths
(\code{config}, \code{workflow}) invisibly.
\item \code{write_biotrace_workflow()}: the absolute path, invisibly.
\item \code{biotrace_workflow_text()}: a character vector (one element per
line, no trailing newline) representing the workflow YAML.
}
}
\description{
\code{use_biotrace()} initialises BioTrace integration in a project by
writing \code{.github/biotrace.yml} and \code{.github/workflows/biotrace.yml}.
}
\details{
\code{write_biotrace_workflow()} writes the workflow file alone.

\code{biotrace_workflow_text()} returns the workflow file content as a
character vector (so callers can inspect or post it without writing
to disk).

The generated workflow references the upstream Action directly:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{uses: rotsl/biotrace@v1
}\if{html}{\out{</div>}}

It never references this package itself as an Action, because
\code{biotrace} is an R package, not a GitHub Action repository.
}
\section{Inputs echoed into the workflow}{

The generated workflow uses these inputs (matching the upstream
\code{action.yml} of BioTrace v1.x): \code{config}, \code{github-token},
\code{fail-on}, \code{ai-enabled}. Other inputs retain their upstream
defaults. Pass \code{extra_inputs} to add or override inputs.
}

\examples{
tmp <- tempfile()
write_biotrace_workflow(tmp)
cat(biotrace_workflow_text(), sep = "\n")

}
