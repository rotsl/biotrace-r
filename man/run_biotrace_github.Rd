% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/github.R
\name{run_biotrace_github}
\alias{run_biotrace_github}
\title{Trigger or inspect BioTrace via GitHub}
\usage{
run_biotrace_github(
  repo,
  action = c("list-workflows", "list-runs", "trigger"),
  workflow_name = "biotrace.yml",
  ref = NULL,
  token = NULL,
  token_env = "GITHUB_TOKEN",
  api_url = "https://api.github.com",
  transport = NULL,
  dry_run = FALSE
)
}
\arguments{
\item{repo}{\verb{[character(1)]} Repository slug, in \code{owner/name} form
(e.g. \code{"rotsl/biotrace-r"}).}

\item{action}{\verb{[character(1)]} What to do. One of:
\itemize{
\item \code{"list-workflows"}: list workflow files in the repo.
\item \code{"list-runs"}: list recent runs of the BioTrace workflow.
\item \code{"trigger"}: dispatch the BioTrace workflow
(\code{workflow_dispatch}). Requires \code{repo} scope.
}}

\item{workflow_name}{\verb{[character(1)]} Workflow file name
(e.g. \code{"biotrace.yml"}). Used for \code{list-runs} and \code{trigger}.}

\item{ref}{\verb{[character(1)]} Git ref to dispatch the workflow on.
Defaults to the repository's default branch when \code{NULL}.}

\item{token}{\verb{[character(1)]} GitHub token. Leave NULL to read
from environment variables (see "Authentication").}

\item{token_env}{\verb{[character(1)]} Name of the environment variable
holding the token. Defaults to \code{"GITHUB_TOKEN"}.}

\item{api_url}{\verb{[character(1)]} GitHub API root. Defaults to
\verb{https://api.github.com}. Override for GitHub Enterprise.}

\item{transport}{\verb{[function]} Custom HTTP transport for testing.
Takes \verb{(url, method, headers, body)} and returns a list with
\code{status}, \code{headers}, \code{content} (raw). The default uses
\code{utils::download.file} for GETs and \code{httr::POST}-equivalent logic
for POSTs, but we do not require \code{httr} as a dependency.}

\item{dry_run}{\verb{[logical(1)]} If \code{TRUE}, return the request that
\emph{would} be issued without sending it. Useful for the CLI's
\code{--dry-run} flag and for tests.}
}
\value{
A list (the parsed JSON response, or the request that would have
been issued when \code{dry_run = TRUE}). The list always contains a
\code{request} element describing what was sent.
}
\description{
\code{run_biotrace_github()} provides an R-facing way to interact with a
GitHub repository that already uses the BioTrace workflow. It does
not embed the upstream BioTrace engine, and it does not modify
\code{rotsl/biotrace}.
}
\details{
The function uses the GitHub REST API via \code{utils::download.file} for
the read-only parts (workflow listing, workflow runs, run
artifacts). For triggering (\code{workflow_dispatch}) it requires a token
with \code{repo} scope (or \code{public_repo} for public repositories).

This helper is intentionally narrow: it does not attempt to
re-implement GitHub Actions, and it does not claim to execute
BioTrace locally. It is a convenience wrapper around the GitHub
REST API for the same workflow that \code{use_biotrace()} writes.
}
\section{Authentication}{

Tokens are read from environment variables only. They are never
written to disk, never embedded into generated workflows, and
never printed. The lookup order is:
\enumerate{
\item The \code{token} argument.
\item The environment variable named by the \code{token_env} argument
(default: \code{GITHUB_TOKEN}).
\item The environment variable \code{GH_TOKEN} (a GitHub CLI convention).
}

If none of those is set, the function aborts with a clear error
message. It does not silently degrade to unauthenticated access.
}

\section{Network behaviour}{

All network access is opt-in. The function never touches the
network during package load, during \verb{R CMD check}, or during any
of the local helpers. The unit tests use a mock transport (the
\code{transport} argument) so they run completely offline.
}

\section{Skip-on-CRAN}{

Tests that exercise the live network path are tagged with
\code{skip_on_cran()}. The default behaviour when \code{transport} is \code{NULL}
and no token is present is to abort cleanly.
}

\examples{
\dontrun{
# Requires GITHUB_TOKEN set.
run_biotrace_github("rotsl/biotrace-r", action = "list-runs")
}

# Safe, offline: see the request that would be issued.
run_biotrace_github(
  "rotsl/biotrace-r",
  action = "trigger",
  dry_run = TRUE
)

}
